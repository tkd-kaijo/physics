ntry(teacher,time){const cell=teacher?.cells?.[time];return cell?.classId?{course:cell.course,subject:cell.subject,classId:cell.classId}:null;}
  function referenceEntry(teacher,time){const key=`${teacher.name}|${time}`;return baselineReference?.entries.has(key)?baselineReference.entries.get(key):sourceEntry(teacher,time);}
  function referenceTeam(slot){if(baselineReference){const members=baselineReference.classTeams.get(`${slot.classId}|${slot.time}`)||[];return{members:members.map(item=>item.name),memberSubjects:Object.fromEntries(members.map(item=>[item.name,item.subject]))};}const team=teams[slot.id];return{members:team.originalMembers.slice(),memberSubjects:{...team.originalMemberSubjects}};}
  function updateReferenceStatus(){const message=baselineReference?`初期状態の比較基準：${baselineFileName}`:'比較基準：現在読み込んだ時間割CSV';baselineStatus.textContent=baselineReference?`比較基準として「${baselineFileName}」を読み込みました。`:'入力しない場合は、現在の時間割CSVを比較基準にします。';referenceStatus.textContent=message;}

  function buildModel(){teams=[];slots=[];slotsByClass=new Map();slotByClassTime=new Map();memberTeams=new Map();fixedBusy=new Map();const byClassTime=new Map();teachers.forEach(teacher=>{fixedBusy.set(teacher.name,new Set());Object.values(teacher.cells).forEach(cell=>{if(!cell.course)return;if(!cell.classId){if(!reserve(cell.course))fixedBusy.get(teacher.name).add(timeKey(cell.day,cell.period));return}const key=`${cell.classId}|${cell.day}|${cell.period}`;if(!byClassTime.has(key))byClassTime.set(key,{classId:cell.classId,day:cell.day,period:cell.period,entries:[]});byClassTime.get(key).entries.push({teacher:teacher.name,subject:cell.subject,course:cell.course})})});[...byClassTime.values()].sort((a,b)=>a.classId.localeCompare(b.classId,'ja')||DAYS.indexOf(a.day)-DAYS.indexOf(b.day)||a.period-b.period).forEach(group=>{const id=teams.length,subjects=[...new Set(group.entries.map(entry=>clean(entry.subject)).filter(Boolean))],memberSubjects=Object.fromEntries(group.entries.map(entry=>[entry.teacher,entry.subject])),members=[...new Set(group.entries.map(entry=>entry.teacher))].sort((a,b)=>a.localeCompare(b,'ja'));teams.push({id,members,subject:subjects.join(' / '),memberSubjects,originalMembers:members.slice(),originalMemberSubjects:{...memberSubjects}});const slot={id:slots.length,classId:group.classId,day:group.day,period:group.period,time:timeKey(group.day,group.period),course:group.entries[0].course};slots.push(slot);slotByClassTime.set(`${slot.classId}|${slot.time}`,slot.id);if(!slotsByClass.has(slot.classId))slotsByClass.set(slot.classId,[]);slotsByClass.get(slot.classId).push(slot.id);teams[id].members.forEach(name=>{if(!memberTeams.has(name))memberTeams.set(name,[]);memberTeams.get(name).push(id)})});}

  function option(value,text){const node=document.createElement('option');node.value=value;node.textContent=text;return node;}
  function fillTeachers(subject,teacher,preferred=teacher.value){const listed=subject.value==='all'?teachers:teachers.filter(item=>item.categories.includes(subject.value));teacher.replaceChildren(...listed.map(item=>option(item.name,item.name)));if(listed.some(item=>item.name===preferred))teacher.value=preferred;}
  function fillDays(day){day.replaceChildren(...DAYS.map(item=>option(item,`${item}曜日`)));}
  function fillPeriods(day,period){const before=Number(period.value),max=PERIODS[day.value]||6;period.replaceChildren(...Array.from({length:max},(_,index)=>option(String(index+1),`${index+1}限`)));if(before>=1&&before<=max)period.value=String(before);}
  function fillLessonPeriods(teacher,day,period){const before=period.value,member=teacherByName.get(teacher.value),periods=member&&currentState?Array.from({length:PERIODS[day.value]||0},(_,index)=>index+1).filter(value=>teamsAt(member.name,timeKey(day.value,value),currentState).length>0):[];period.replaceChildren(...(periods.length?periods.map(value=>{const teamId=teamsAt(member.name,timeKey(day.value,value),currentState)[0],slot=teamId===undefined?null:slots[currentState.pos[teamId]];return option(String(value),`${value}限　${slot?.classId||''}`);}):[option('','授業なし')]));period.disabled=!periods.length;if(periods.includes(Number(before)))period.value=before;}
  function fillFreePeriods(teacher,day,period){const before=period.value,member=teacherByName.get(teacher.value),periods=member&&currentState?Array.from({length:PERIODS[day.value]||0},(_,index)=>index+1).filter(value=>{const time=timeKey(day.value,value);return teacherAvailableAt(member,{day:day.value,period:value})&&teamsAt(member.name,time,currentState).length===0&&!fixedBusy.get(member.name).has(time)}):[];period.replaceChildren(...(periods.length?periods.map(value=>option(String(value),`${value}限`)):[option('','空きコマなし')]));period.disabled=!periods.length;if(periods.includes(Number(before)))period.value=before;}
  function selection(teacher,day,period){const member=teacherByName.get(teacher.value),dayValue=day.value,periodValue=Number(period.value);if(!member||!dayValue||!periodValue||!currentState)return null;const time=timeKey(dayValue,periodValue),teamId=teamsAt(member.name,time,currentState),team=teamId.length?teams[teamId[0]]:null,slot=team?slots[currentState.pos[team.id]]:null;return{teacher:member,day:dayValue,period:periodValue,time,teamId:team?.id,cell:team?{classId:slot.classId,course:slot.course,subject:team.memberSubjects?.[member.name]||team.subject}: {classId:'',course:'',subject:''}};}
  function setMessage(element,text,error=false){element.textContent=text;element.classList.toggle('error',error);}

  function initial(){return{assign:slots.map((_,index)=>index),pos:teams.map((_,index)=>index),steps:[]};}
  function workingState(){return{assign:currentState.assign.slice(),pos:currentState.pos.slice(),steps:[]};}
  function teamsAt(name,time,state){return(memberTeams.get(name)||[]).filter(id=>slots[state.pos[id]].time===time);}
  function teacherAvailableAt(teacher,slot){if(!teacher||!teacher.workingDays[slot.day])return false;if(teacher.fullTime)return true;const window=teacher.workWindows[slot.day];return Boolean(window&&slot.period>=window.first&&slot.period<=window.last);}
  function placeFree(team,target,state,leaving){return team.members.every(name=>{const teacher=teacherByName.get(name),occupied=teamsAt(name,target.time,state).filter(id=>id!==leaving);return teacherAvailableAt(teacher,target)&&!fixedBusy.get(name).has(target.time)&&occupied.length===0});}
  function rawSwap(state,firstSlot,secondSlot,step){const first=state.assign[firstSlot],second=state.assign[secondSlot],assign=state.assign.slice(),pos=state.pos.slice();assign[firstSlot]=second;assign[secondSlot]=first;pos[first]=secondSlot;pos[second]=firstSlot;return{assign,pos,steps:step?[...state.steps,step]:state.steps};}
  function conflictTeams(state){const result=new Set();teachers.forEach(teacher=>{const occupied=new Map();(memberTeams.get(teacher.name)||[]).forEach(id=>{const time=slots[state.pos[id]].time;if(!occupied.has(time))occupied.set(time,[]);occupied.get(time).push(id)});fixedBusy.get(teacher.name).forEach(time=>{if(!occupied.has(time))occupied.set(time,[]);occupied.get(time).push(-1)});occupied.forEach(ids=>{if(ids.length>1)ids.filter(id=>id>=0).forEach(id=>result.add(id))})});teams.forEach(team=>{const slot=slots[state.pos[team.id]];if(team.members.some(name=>!teacherAvailableAt(teacherByName.get(name),slot)))result.add(team.id)});return result;}
  function sameMembers(first,second){return teams[first].members.some(name=>teams[second].members.includes(name));}
  function canSwap(state,firstSlot,secondSlot,{locked=new Set(),differentTeachers=false}={}){const first=state.assign[firstSlot],second=state.assign[secondSlot];if(locked.has(first)||locked.has(second))return false;if(differentTeachers&&sameMembers(first,second))return false;return placeFree(teams[first],slots[secondSlot],state,second)&&placeFree(teams[second],slots[firstSlot],state,first);}
  function adjustmentOperations(state,{locked=new Set(),differentTeachers=false,active=conflictTeams(state)}={}){const choices=new Map();active.forEach(id=>{if(locked.has(id))return;const source=state.pos[id],classId=slots[source].classId;(slotsByClass.get(classId)||[]).forEach(destination=>{if(destination===source||!canSwap(state,source,destination,{locked,differentTeachers}))return;choices.set([source,destination].sort((a,b)=>a-b).join(':'),{source,destination})})});return[...choices.values()];}
  const signature=state=>state.assign.join(',');
  function solve(start,accept,operations){const queue=[start],visited=new Set([signature(start)]),found=[],started=Date.now();let cursor=0,limited=false;while(cursor<queue.length&&found.length<MAX_SOLUTIONS){if(Date.now()-started>=SEARCH_LIMIT_MS){limited=true;break}const state=queue[cursor++];if(accept(state)){found.push(state);continue}if(state.steps.length>=MAX_DEPTH){limited=true;continue}const choices=operations(state);if(choices.length>MAX_BRANCHES)limited=true;for(const choice of choices.slice(0,MAX_BRANCHES)){if(queue.length>=MAX_STATES||Date.now()-started>=SEARCH_LIMIT_MS){limited=true;break}const first=state.assign[choice.source],second=state.assign[choice.destination],step={kind:'調整交換',classId:slots[choice.source].classId,first,firstSlot:choice.source,firstText:teamText(teams[first]),second,secondSlot:choice.destination,secondText:teamText(teams[second])},next=rawSwap(state,choice.source,choice.destination,step),key=signature(next);if(!visited.has(key)){visited.add(key);queue.push(next)}}if(queue.length>=MAX_STATES||Date.now()-started>=SEARCH_LIMIT_MS)break}return{found,limited};}
  const teamText=team=>`${team.members.map(name=>`${name}先生`).join('・')}　${team.subject||'授業'}`;

  function clearResults(){resultPanel.classList.add('hidden');solutionsEl.replaceChildren();}
  function appendStepRows(body,steps){steps.forEach(step=>{const row=document.createElement('tr');if(step.kind==='指定譲渡'){[step.kind,step.classId,step.lesson,timeLabel(step.day,step.period),step.target,''].forEach(value=>{const cell=document.createElement('td');cell.textContent=value;row.append(cell)})}else{const firstSlot=slots[step.firstSlot],secondSlot=slots[step.secondSlot],[firstText,secondText]=[step.firstText||teamText(teams[step.first]),step.secondText||teamText(teams[step.second])];[step.kind,step.classId,firstText,timeLabel(firstSlot.day,firstSlot.period),secondText,timeLabel(secondSlot.day,secondSlot.period)].forEach(value=>{const cell=document.createElement('td');cell.textContent=value;row.append(cell)})}body.append(row)})}
  function sequenceTable(steps){const table=document.createElement('table'),head=document.createElement('thead'),body=document.createElement('tbody');head.innerHTML='<tr><th>手順</th><th>組</th><th>授業</th><th>時限</th><th>譲渡先・交換先</th><th>時限</th></tr>';appendStepRows(body,steps);table.append(head,body);return table;}
  function snapshotCurrent(){return{state:{assign:currentState.assign.slice(),pos:currentState.pos.slice()},history:history.slice(),teams:teams.map(team=>({members:team.members.slice(),memberSubjects:{...team.memberSubjects},subject:team.subject}))};}
  function restoreSnapshot(snapshot){snapshot.teams.forEach((saved,index)=>{teams[index].members=saved.members.slice();teams[index].memberSubjects={...saved.memberSubjects};teams[index].subject=saved.subject});memberTeams=new Map();teams.forEach(team=>team.members.forEach(name=>memberTeams.set(name,[...(memberTeams.get(name)||[]),team.id])));currentState={assign:snapshot.state.assign.slice(),pos:snapshot.state.pos.slice(),steps:[]};history=snapshot.history.slice();}
  function showHistory(){historyPanel.classList.toggle('hidden',history.length===0);historySteps.replaceChildren();historyStatus.textContent=`${history.length}件の譲渡・交換を実行済みです。`;if(history.length)historySteps.append(sequenceTable(history));undoButton.disabled=undoStack.length===0;finalPanel.classList.remove('hidden');}
  function executePlan(state,steps,commit){undoStack.push(snapshotCurrent());commit?.();currentState={assign:state.assign.slice(),pos:state.pos.slice(),steps:[]};history.push(...steps);clearResults();showHistory();}
  function undoLast(){const snapshot=undoStack.pop();if(!snapshot)return;restoreSnapshot(snapshot);clearResults();updateAllSummaries();showHistory();}
  function render(result,{forced=[],title='調整案',empty='調整案は見つかりませんでした。',intro='表を上から順に実施してください。',commit}={}){resultPanel.classList.remove('hidden');$('result-heading').textContent=title;solutionsEl.replaceChildren();if(!result.found.length){resultStatus.textContent=empty;return}resultStatus.textContent=`${result.found.length}パターン見つかりました。${intro}${result.limited?' 探索上限に達したため、他のパターンがある可能性があります。':''}`;result.found.forEach((state,index)=>{const section=document.createElement('section');section.className='solution';const heading=document.createElement('div');heading.className='summary';const headingText=document.createElement('h3');headingText.textContent=`パターン ${index+1}`;const count=document.createElement('span');count.className='pill';count.textContent=forced.length?`指定操作＋${state.steps.length}回調整`:`${state.steps.length}回交換`;heading.append(headingText,count);section.append(heading);section.append(sequenceTable([...history,...forced,...state.steps]));const actions=document.createElement('div'),button=document.createElement('button');actions.className='actions';button.className='primary';button.type='button';button.textContent='実行して、次の譲渡・交換を行う';button.addEventListener('click',()=>executePlan(state,[...forced,...state.steps],commit));actions.append(button);section.append(actions);solutionsEl.append(section)})}

  function updateTransferSummary(){transfer.summary.replaceChildren();setMessage(transfer.message,'');const source=selection(transfer.giverTeacher,transfer.day,transfer.period),receiver=teacherByName.get(transfer.receiverTeacher.value);if(!source||!receiver)return;const text=document.createElement('strong');text.textContent=`${source.teacher.name}先生の${timeLabel(source.day,source.period)}の授業を ${receiver.name}先生へ譲渡`;transfer.summary.append(text);if(source.cell.classId){const pill=document.createElement('span');pill.className='pill';pill.textContent=source.cell.subject?`${source.cell.classId}・${source.cell.subject}`:source.cell.classId;transfer.summary.append(pill);}}
  function receiverTeam(receiver,classId){const teamId=(memberTeams.get(receiver.name)||[]).find(id=>slots[currentState.pos[id]].classId===classId);if(teamId===undefined)return{members:[receiver.name],memberSubjects:{[receiver.name]:''},subject:''};const team=teams[teamId];return{members:team.members.slice(),memberSubjects:{...team.memberSubjects},subject:team.subject};}
  function transferTeam(teamId,recipients){const team=teams[teamId],snapshot={members:team.members.slice(),memberSubjects:{...team.memberSubjects},subject:team.subject};team.members.forEach(name=>memberTeams.set(name,(memberTeams.get(name)||[]).filter(id=>id!==teamId)));team.members=recipients.members.slice();team.memberSubjects={...recipients.memberSubjects};team.subject=recipients.subject;team.members.forEach(name=>memberTeams.set(name,[...(memberTeams.get(name)||[]),teamId]));return snapshot;}
  function restoreTransfer(teamId,snapshot){const team=teams[teamId];team.members.forEach(name=>memberTeams.set(name,(memberTeams.get(name)||[]).filter(id=>id!==teamId)));team.members=snapshot.members.slice();team.memberSubjects={...snapshot.memberSubjects};team.subject=snapshot.subject;team.members.forEach(name=>memberTeams.set(name,[...(memberTeams.get(name)||[]),teamId]));}
  function transferOperations(state,teamId,recipients,locked){return adjustmentOperations(state,{locked,differentTeachers:true,active:conflictTeams(state)});}
  function searchTransfer(){const source=selection(transfer.giverTeacher,transfer.day,transfer.period),receiver=teacherByName.get(transfer.receiverTeacher.value);clearResults();setMessage(transfer.message,'');if(!source||!receiver)return;if(!source.cell.classId)return setMessage(transfer.message,'組の授業コマを選んでください。',true);if(source.teacher.name===receiver.name)return setMessage(transfer.message,'譲渡する教員と受け取る教員には、異なる教員を選んでください。',true);const state=workingState(),teamId=source.teamId;if(teamId===undefined)return setMessage(transfer.message,'組の授業コマを選んでください。',true);if(teams[teamId].members.includes(receiver.name))return setMessage(transfer.message,'受け取る教員は、すでにこの授業の担当です。',true);const givers=teams[teamId].members.slice(),recipients=receiverTeam(receiver,source.cell.classId);if(recipients.members.some(name=>givers.includes(name)))return setMessage(transfer.message,'受取側の共同担当者に、元の担当教員が含まれています。',true);const initialSlot=slots[state.pos[teamId]],snapshot=transferTeam(teamId,recipients),mustMove=recipients.members.some(name=>!teacherAvailableAt(teacherByName.get(name),initialSlot)),locked=mustMove?new Set():new Set([teamId]),result=solve(state,current=>{const finalSlot=slots[current.pos[teamId]];return conflictTeams(current).size===0&&recipients.members.every(name=>teacherAvail