ableAt(teacherByName.get(name),finalSlot))&&(!mustMove||finalSlot.time!==source.time)&&givers.every(name=>teamsAt(name,source.time,current).length===0)},current=>transferOperations(current,teamId,recipients,locked));restoreTransfer(teamId,snapshot);setMessage(transfer.message,mustMove?'指定譲渡を仮置きし、受取側全員の勤務時間内に移す交換を探索しました。':'指定譲渡で生じる重複授業を探索しました。');render(result,{title:'コマの譲渡案',empty:'重複授業を解消する譲渡案は見つかりませんでした。',intro:'指定譲渡の後、表を上から順に実施してください。',commit:()=>transferTeam(teamId,recipients),forced:[{kind:'指定譲渡',classId:source.cell.classId,lesson:`${givers.map(name=>`${name}先生`).join('・')}　${source.cell.subject||'授業'}`,day:source.day,period:source.period,target:`${recipients.members.map(name=>`${name}先生`).join('・')}へ譲渡`} ]});}

  function crossSelection(teacher,day,period){return selection(teacher,day,period);}
  function updateCrossSummary(){cross.summary.replaceChildren();setMessage(cross.message,'');const first=crossSelection(cross.teacherA,cross.dayA,cross.periodA),second=crossSelection(cross.teacherB,cross.dayB,cross.periodB);if(!first||!second)return;const text=document.createElement('strong');text.textContent=`${first.teacher.name}先生 ${timeLabel(first.day,first.period)}　⇄　${second.teacher.name}先生 ${timeLabel(second.day,second.period)}`;cross.summary.append(text);[first,second].forEach(item=>{if(item.cell.classId){const pill=document.createElement('span');pill.className='pill';pill.textContent=item.cell.subject?`${item.cell.classId}・${item.cell.subject}`:item.cell.classId;cross.summary.append(pill)}});}
  function searchCross(){const first=crossSelection(cross.teacherA,cross.dayA,cross.periodA),second=crossSelection(cross.teacherB,cross.dayB,cross.periodB);clearResults();setMessage(cross.message,'');if(!first||!second)return;if(!first.cell.classId||!second.cell.classId)return setMessage(cross.message,'2つとも組の授業コマを選んでください。',true);if(first.teacher.name===second.teacher.name||first.time===second.time)return setMessage(cross.message,'異なる教員・異なる時限の授業コマを選んでください。',true);const state=workingState(),firstTeam=first.teamId,secondTeam=second.teamId;if(firstTeam===undefined||secondTeam===undefined)return setMessage(cross.message,'選択した授業を特定できません。',true);if(sameMembers(firstTeam,secondTeam))return setMessage(cross.message,'同時担当を含む同じ教員どうしの交換はできません。',true);const firstSlot=state.pos[firstTeam],secondSlot=state.pos[secondTeam],forced={kind:'指定交換',classId:`${first.cell.classId} ⇄ ${second.cell.classId}`,first:firstTeam,firstSlot,second:secondTeam,secondSlot};const result=solve(rawSwap(state,firstSlot,secondSlot,null),current=>conflictTeams(current).size===0,current=>adjustmentOperations(current,{differentTeachers:true}));setMessage(cross.message,'指定交換で生じる重複授業を探索しました。');render(result,{title:'コマの交換案',empty:'重複授業を解消する交換案は見つかりませんでした。',intro:'指定交換の後、表を上から順に実施してください。',forced:[forced]});}

  function currentMove(){const teacher=teacherByName.get(move.teacher.value),sourceDay=move.sourceDay.value,sourcePeriod=Number(move.sourcePeriod.value),targetDay=move.targetDay.value,targetPeriod=Number(move.targetPeriod.value);return teacher&&sourceDay&&sourcePeriod&&targetDay&&targetPeriod?{teacher,sourceDay,sourcePeriod,targetDay,targetPeriod,sourceTime:timeKey(sourceDay,sourcePeriod),targetTime:timeKey(targetDay,targetPeriod)}:null;}
  function updateMoveSummary(){move.summary.replaceChildren();setMessage(move.message,'');const request=currentMove();if(!request)return;const cell=request.teacher.cells[request.sourceTime],text=document.createElement('strong');text.textContent=`${request.teacher.name}先生　${timeLabel(request.sourceDay,request.sourcePeriod)} → ${timeLabel(request.targetDay,request.targetPeriod)}`;move.summary.append(text);if(cell.classId){const pill=document.createElement('span');pill.className='pill';pill.textContent=cell.subject?`${cell.classId}・${cell.subject}`:cell.classId;move.summary.append(pill);}}
  function searchMove(){const request=currentMove();clearResults();setMessage(move.message,'');if(!request)return;const sourceCell=selection(move.teacher,move.sourceDay,move.sourcePeriod).cell;if(!sourceCell.classId)return setMessage(move.message,'授業コマには組の授業を選んでください。',true);if(request.sourceTime===request.targetTime)return setMessage(move.message,'授業コマと空きコマを異なるコマにしてください。',true);const state=workingState(),sourceTeam=teamsAt(request.teacher.name,request.sourceTime,state)[0],sourceSlot=state.pos[sourceTeam],targetSlot=slotByClassTime.get(`${sourceCell.classId}|${request.targetTime}`);if(targetSlot===undefined)return setMessage(move.message,`${sourceCell.classId}には${timeLabel(request.targetDay,request.targetPeriod)}の授業がないため、交換先を作れません。`,true);const targetTeam=state.assign[targetSlot];if(!placeFree(teams[sourceTeam],slots[targetSlot],state,targetTeam))return setMessage(move.message,'空きコマには、同時担当を含む担当教員全員の空きコマを選んでください。',true);const forced={kind:'指定移動',classId:sourceCell.classId,first:sourceTeam,firstSlot:sourceSlot,second:targetTeam,secondSlot:targetSlot},forcedState=rawSwap(state,sourceSlot,targetSlot,null),locked=new Set([sourceTeam]);const result=solve(forcedState,current=>conflictTeams(current).size===0&&current.pos[sourceTeam]===targetSlot,current=>adjustmentOperations(current,{locked}));setMessage(move.message,`${sourceCell.classId}の${timeLabel(request.sourceDay,request.sourcePeriod)}と${timeLabel(request.targetDay,request.targetPeriod)}を入れ替え、発生する重複授業を探索しました。`);render(result,{title:'授業コマと空きコマの交換案',empty:'重複授業を解消する移動案は見つかりませんでした。',intro:'指定移動の後、表を上から順に実施してください。',forced:[forced]});}

  function updateAllSummaries(){updateTransferSummary();updateCrossSummary();updateMoveSummary();}
  function fillControls(){[transfer.giverSubject,transfer.receiverSubject,cross.subjectA,cross.subjectB,move.subject].forEach(subject=>{subject.value='all'});const firstTeacher=teachers[0]?.name||'';[transfer.day,cross.dayA,cross.dayB,move.sourceDay,move.targetDay].forEach(fillDays);fillTeachers(transfer.giverSubject,transfer.giverTeacher,firstTeacher);fillTeachers(transfer.receiverSubject,transfer.receiverTeacher,firstTeacher);fillTeachers(cross.subjectA,cross.teacherA,'中村陽');fillTeachers(cross.subjectB,cross.teacherB,'川崎');fillTeachers(move.subject,move.teacher,'中村陽');transfer.day.value='月';cross.dayA.value='月';cross.dayB.value='木';move.sourceDay.value='月';move.targetDay.value='月';[[transfer.giverTeacher,transfer.day,transfer.period],[cross.teacherA,cross.dayA,cross.periodA],[cross.teacherB,cross.dayB,cross.periodB],[move.teacher,move.sourceDay,move.sourcePeriod]].forEach(([teacher,day,period])=>fillLessonPeriods(teacher,day,period));fillFreePeriods(move.teacher,move.targetDay,move.targetPeriod);updateAllSummaries();}
  function activate(name){Object.entries(tabs).forEach(([key,tab])=>tab.setAttribute('aria-selected',String(key===name)));Object.entries(panels).forEach(([key,panel])=>panel.classList.toggle('hidden',key!==name));clearResults();}
  function decodeFile(file){return file.arrayBuffer().then(buffer=>{const bytes=new Uint8Array(buffer),utf8=new TextDecoder('utf-8').decode(bytes),shiftJis=new TextDecoder('shift-jis').decode(bytes);return(utf8.match(/[\uFFFD]/g)||[]).length<=(shiftJis.match(/[\uFFFD]/g)||[]).length?utf8:shiftJis;});}
  const csvCell=value=>{const text=String(value??'');return /[",\r\n]/.test(text)?`"${text.replace(/"/g,'""')}"`:text;};
  function exportCompletedCsv(){if(!currentState)return;const rows=sourceRows.map(row=>row.slice()),entries=new Map();teams.forEach(team=>{const slot=slots[currentState.pos[team.id]];team.members.forEach(name=>entries.set(`${name}|${slot.time}`,{course:slot.course,subject:team.memberSubjects?.[name]||team.subject}))});teachers.forEach(teacher=>{DAYS.forEach((day,dayIndex)=>{for(let period=1;period<=PERIODS[day];period+=1){const time=timeKey(day,period),column=1+dayIndex*6+period-1,entry=entries.get(`${teacher.name}|${time}`),original=teacher.cells[time];if(original.classId||entry){rows[teacher.rowIndex][column]=entry?entry.course:'';rows[teacher.rowIndex+1][column]=entry?entry.subject:''}}});});const text=`\uFEFF${rows.map(row=>row.map(csvCell).join(',')).join('\r\n')}\r\n`,url=URL.createObjectURL(new Blob([text],{type:'text/csv;charset=utf-8'})),link=document.createElement('a');link.href=url;link.download='時間割調整後.csv';link.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
  const escapeHtml=value=>String(value??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
  function finalEntries(){const entries=new Map();teams.forEach(team=>{const slot=slots[currentState.pos[team.id]];team.members.forEach(name=>entries.set(`${name}|${slot.time}`,{course:slot.course,subject:team.memberSubjects?.[name]||team.subject}))});return entries;}
  const entryChanged=(entry,reference)=>((entry?.course||'')!==(reference?.course||'')||(entry?.subject||'')!==(reference?.subject||''));
  function changedTeachers(entries){return teachers.filter(teacher=>DAYS.some(day=>Array.from({length:PERIODS[day]},(_,index)=>{const time=timeKey(day,index+1),entry=entries.get(`${teacher.name}|${time}`);return entryChanged(entry,referenceEntry(teacher,time));}).some(Boolean))).map(teacher=>teacher.name);}
  function classSlotChanged(slotId){const slot=slots[slotId],team=teams[currentState.assign[slotId]],reference=referenceTeam(slot);return lessonSignature(team.members,team.memberSubjects)!==lessonSignature(reference.members,reference.memberSubjects);}
  function changedClasses(){const changed=new Set();slots.forEach(slot=>{if(classSlotChanged(slot.id))changed.add(slot.classId)});return changed;}
  function printableTable(title,rows,changed){return `<section class="print-page"><h1>完成版時間割</h1><h2>${escapeHtml(title)}</h2><table><thead><tr><th></th>${DAYS.map(day=>`<th>${day}</th>`).join('')}</tr></thead><tbody>${rows.map((row,index)=>`<tr><th>${index+1}</th>${row.map((cell,dayIndex)=>`<td${changed?.[index]?.[dayIndex]?' class="changed"':''}>${cell}</td>`).join('')}</tr>`).join('')}</tbody></table></section>`;}
  function teacherPrintPages(entries){return changedTeachers(entries).map(name=>{const teacher=teacherByName.get(name),changed=Array.from({length:6},(_,period)=>DAYS.map(day=>{if(period>=PERIODS[day])return false;const time=timeKey(day,period+1);return entryChanged(entries.get(`${name}|${time}`),referenceEntry(teacher,time));})),rows=Array.from({length:6},(_,period)=>DAYS.map(day=>{if(period>=PERIODS[day])return '';const time=timeKey(day,period+1),entry=entries.get(`${name}|${time}`);if(entry)return `${escapeHtml(entry.course)}<br>${escapeHtml(entry.subject)}`;const original=teacher.cells[time];return original.classId?'':`${escapeHtml(original.course)}${original.subject?`<br>${escapeHtml(original.subject)}`:''}`;}));return printableTable(`${name}先生`,rows,changed)});}
  function classPrintPages(){return [...changedClasses()].sort((a,b)=>a.localeCompare(b,'ja')).map(classId=>{const classSlots=new Map((slotsByClass.get(classId)||[]).map(id=>[slots[id].time,id])),changed=Array.from({length:6},(_,period)=>DAYS.map(day=>{const slotId=classSlots.get(timeKey(day,period+1));return slotId!==undefined&&classSlotChanged(slotId);})),rows=Array.from({length:6},(_,period)=>DAYS.map(day=>{if(period>=PERIODS[day])return '';const slotId=classSlots.get(timeKey(day,period+1));if(slotId===undefined)return '';const team=teams[currentState.assign[slotId]];return `${escapeHtml(team.subject)}<br>${escapeHtml(team.members.join('・'))}`;}));const display=slots[slotsByClass.get(classId)[0]].course;return printableTable(display,rows,changed)});}
  function openPrintablePdf(kind){if(!currentState)return;const entries=finalEntries(),pages=kind==='teacher'?teacherPrintPages(entries):classPrintPages();if(!pages.length){window.alert('変更があった時間割はありません。');return}const title=kind==='teacher'?'完成版先生時間割':'完成版クラス時間割',printWindow=window.open('','_blank');if(!printWindow){window.alert('PDF出力画面を開けませんでした。ポップアップを許可してください。');return}printWindow.document.open();printWindow.document.write(`<!doctype html><html lang="ja"><head><meta charset="utf-8"><title>${title}</title><style>@page{size:A4 landscape;margin:13mm}body{font-family:-apple-system,BlinkMacSystemFont,"Hiragino Sans","Yu Gothic",sans-serif;color:#111;margin:0}h1{font-size:17px;font-weight:500;margin:0 0 42px}h2{text-align:center;font-size:19px;font-weight:500;margin:0 0 4px}table{border-collapse:collapse;width:100%;table-layout:fixed;font-size:15px}th,td{border:1px solid #111;text-align:center;vertical-align:top;padding:3px 4px;white-space:pre-line}thead th{font-size:17px;padding:2px}tbody th{width:34px;vertical-align:middle;font-weight:400}td{height:70px;text-align:left;line-height:1.35}.changed{border:3px solid #111!important}.print-page{break-after:page}.print-page:last-child{break-after:auto}@media print{h1{margin-top:0}}</style></head><body>${pages.join('')}</body></html>`);printWindow.document.close();setTimeout(()=>{printWindow.focus();printWindow.print();},250);}

  fileInput.addEventListener('change',async()=>{const file=fileInput.files[0];if(!file)return;try{loadStatus.classList.remove('error');loadStatus.textContent='読み込み中…';const text=await decodeFile(file);sourceRows=parseCsv(text.replace(/^\uFEFF/,''));teachers=parseTimetable(text);teacherByName=new Map(teachers.map(teacher=>[teacher.name,teacher]));buildModel();currentState=initial();history=[];undoStack=[];finalPanel.classList.remove('hidden');historyPanel.classList.add('hidden');fillControls();appPanel.classList.remove('hidden');activate('transfer');updateReferenceStatus();loadStatus.textContent=`${teachers.length}名の時間割を読み込みました。`;}catch(error){teachers=[];currentState=null;appPanel.classList.add('hidden');loadStatus.classList.add('error');loadStatus.textContent=`読み込めませんでした：${error.message}`;}});
  baselineFileInput.addEventListener('change',async()=>{const file=baselineFileInput.files[0];if(!file)return;try{baselineStatus.classList.remove('error');baselineStatus.textContent='初期状態のCSVを読み込み中…';const text=await decodeFile(file);baselineReference=createReference(parseTimetable(text));baselineFileName=file.name;updateReferenceStatus();}catch(error){baselineReference=null;baselineFileName='';baselineStatus.classList.add('error');baselineStatus.textContent=`初期状態のCSVを読み込めませんでした：${error.message}`;referenceStatus.textContent='比較基準：現在読み込んだ時間割CSV';}});
  Object.entries(tabs).forEach(([name,tab])=>tab.addEventListener('click',()=>activate(name)));
  exportButton.addEventListener('click',exportCompletedCsv);
  undoButton.addEventListener('click',undoLast);
  teacherPdfButton.addEventListener('click',()=>openPrintablePdf('teacher'));
  classPdfButton.addEventListener('click',()=>openPrintablePdf('class'));
  transfer.giverSubject.addEventListener('change',()=>{fillTeachers(transfer.giverSubject,transfer.giverTeacher);fillLessonPeriods(transfer.giverTeacher,transfer.day,transfer.period);updateTransferSummary();});transfer.receiverSubject.addEventListener('change',()=>{fillTeachers(transfer.receiverSubject,transfer.receiverTeacher);updateTransferSummary();});transfer.giverTeacher.addEventListener('change',()=>{fillLessonPeriods(transfer.giverTeacher,transfer.day,transfer.period);updateTransferSummary();});transfer.day.addEventListener('change',()=>{fillLessonPeriods(transfer.giverTeacher,transfer.day,transfer.period);updateTransferSummary();});transfer.period.addEventListener('change',updateTransferSummary);transfer.receiverTeacher.addEventListener('change',updateTransferSummary);transfer.button.addEventListener('click',searchTransfer);
  [[cross.subjectA,cross.teacherA,cross.dayA,cross.periodA],[cross.subjectB,cross.teacherB,cross.dayB,cross.periodB]].forEach(([subject,teacher,day,period])=>{subject.addEventListener('change',()=>{fillTeachers(subject,teacher);fillLessonPeriods(teacher,day,period);updateCrossSummary();});teacher.addEventListener('change',()=>{fillLessonPeriods(teacher,day,period);updateCrossSummary();});day.addEventListener('change',()=>{fillLessonPeriods(teacher,day,period);updateCrossSummary();});period.addEventListener('change',updateCrossSummary)});cross.button.addEventListener('click',searchCross);
  move.subject.addEventListener('change',()=>{fillTeachers(move.subject,move.teacher);fillLessonPeriods(move.teacher,move.sourceDay,move.sourcePeriod);fillFreePeriods(move.teacher,move.targetDay,move.targetPeriod);updateMoveSummary();});move.teacher.addEventListener('change',()=>{fillLessonPeriods(move.teacher,move.sourceDay,move.sourcePeriod);fillFreePeriods(move.teacher,move.targetDay,move.targetPeriod);updateMoveSummary();});move.sourceDay.addEventListener('change',()=>{fillLessonPeriods(move.teacher,move.sourceDay,move.sourcePeriod);updateMoveSummary();});move.sourcePeriod.addEventListener('change',updateMoveSummary);move.targetDay.addEventListener('change',()=>{fillFreePeriods(move.teacher,move.targetDay,move.targetPeriod);updateMoveSummary();});move.targetPeriod.addEventListener('change',updateMoveSummary);move.button.addEventListener('click',searchMove);
  const submissionRequestDate=$('